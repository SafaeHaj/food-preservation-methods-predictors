"""Feature engineering (pillars, design matrix, diagnostics).

Relocated as-is from the survival pipeline. These modules still target the old
``t_failure``/``event`` schema and are not yet wired to the measurements schema, so nothing
is imported eagerly here -- import the individual modules directly once they are ported.
"""
