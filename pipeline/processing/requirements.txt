# Processing: the LLM seam, the vocabulary, normalisation, record assembly and the dataset.
# No Docling/Paddle here -- extraction owns documents, this service owns meaning. Base
# web/db deps come from `shared`.
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
httpx>=0.27.0

# The controlled vocabulary loader, and the treatment/application pattern tables beside it.
PyYAML>=6.0

# LLM providers. Ollama needs none (the stdlib talks to its native API); the rest are
# imported lazily, so a provider that is not selected costs nothing at startup.
google-genai>=0.8.0
openai>=1.30.0
anthropic>=0.28.0

pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0
openpyxl>=3.1.0
pyarrow>=16.0.0

# async jobs
redis>=5.0.0
