# Gateway: auth + routing only. FastAPI/pydantic/sqlalchemy/jose/bcrypt/psycopg come from
# the `shared` package (pipeline/shared/pyproject.toml).
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
