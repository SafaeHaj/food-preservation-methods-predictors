"""Gateway business logic.

The gateway owns the platform domain -- authentication, projects, membership, jobs and the
audit trail. Everything scientific belongs to a domain service and is proxied.
"""
