# Extraction: PDF + LLM + Docling/PaddleOCR pipeline. Base web/db deps come from `shared`.
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
aiofiles>=23.2.0
httpx>=0.27.0

Pillow>=10.0.0
pandas>=2.2.0
numpy>=1.26.0
openpyxl>=3.1.0

# Silver: table cleaning, header promotion and footnote stripping. Kept rather than
# rewritten in pandas — it is several hundred lines of tuned behaviour, and a rewrite is
# where the port would acquire its bugs.
polars>=1.0.0
# The controlled vocabulary loader. Only transitive via docling today, which is not a
# dependency to rely on.
PyYAML>=6.0

# No LLM SDKs: every model call moved to processing with the vocabulary and the gold stage.
# Extraction reads documents and emits observations with their labels unresolved, which is
# the whole point of the cut -- it needs no key and no provider.

# async jobs
redis>=5.0.0

# Docling PDF extraction pipeline
# Install GPU-capable PaddlePaddle separately if CUDA is available:
#   pip install paddlepaddle-gpu  (see https://www.paddlepaddle.org.cn/install)
docling>=2.14.0,<3.0
paddleocr>=2.7.0
paddlepaddle>=2.6.0
