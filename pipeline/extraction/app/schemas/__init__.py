"""Response models for the extraction API.

Replaces the hand-built dicts that were assembled inside route modules, where the shape
was implicit, undocumented and free to drift between the three endpoints that returned
"an asset".
"""
